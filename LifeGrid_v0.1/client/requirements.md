## Packages
idb | IndexedDB wrapper for client-side storage
date-fns | Date manipulation and week calculations
recharts | Charts for mood and life statistics
framer-motion | Smooth animations for the grid and modals
lucide-react | Icons (already in stack but confirming usage)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
